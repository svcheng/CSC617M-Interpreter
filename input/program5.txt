main: {
    var flag_str = "true";   
    var flag = true;        

    var ch = 'A';
    var msg = "Hello";

    repeat {
        scan(flag_str);
        flag = cast(flag_str, "bool");
        print(msg);
    } until (!flag);

    if (flag || false) {
        print(ch);
    }
}